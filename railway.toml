[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build --workspace=@vel-ai/shared && npm run build --workspace=@vel-ai/api"

[deploy]
startCommand = "node backend/api/dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
